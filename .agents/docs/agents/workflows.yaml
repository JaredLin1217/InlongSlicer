schema: agents-workflows/v2
purpose: "Core route, employee, deploy, runtime evidence, scoring, handoff."
owns: "Workflow behavior and closeout."
read_first: ["ai-runtime","policy","verify","schemas","deploy when target writes"]
startup:
  controller: "Classify from the compact route index; answer-only no command; inspect before edit/git/deploy/runtime claim; expand named route only."
  common_task_matrix:
    answer_only: "Rules only; no command/current claim."
    scoped_edit: "Target read, scoped diff, focused verify."
    deploy_or_release: "deploy.yaml + release_deploy_push_audit."
    multi_agent_batch_check: "Ack, lifecycle, cleanup, runtime_multi_agent."
    enterprise_dispatch: "Leaders only; org/model-policy/dispatch."
    workflow_artifact: "Artifact evidence; runtime local."
    context_compact: "latest request, route, changes, verify, risks, employees, next."
    context_intelligence: "Resolve at most three authoritative files, impact paths, gaps, and verification level."
    collaborator_window: "Named sessions map to leader collaborators; thread ids runtime-local."
    core_system: "Boundary, canonical set, blocklist, deploy/release contract."
    runtime_execution: "Run, approval, tool/result, cleanup evidence."
    provider_adapter: "Provider capability and tier mapping."
    route_pack: "Deterministic minimal manifest."
    knowledge_footprint: "Cross-window resume evidence."
  fast_classification:
    answer_only: "No state/deploy/runtime claim."
    current_state: "Inspect named now/current/latest/clean/dirty/committed/pushed/tagged/deployed."
    scoped_edit: "Status, target, patch, diff, verify."
    multi_agent_runtime: "Explicit hire/delegate; needs ack, close proof, residue-zero."
    enterprise_dispatch: "Org dispatch, department command, delegation, tier binding."
    workflow_artifact: "Create/verify/collect/simulate/normalize artifacts."
    context_compact: "Compact/resume/handoff/closeout."
    context_intelligence: "Task intent, minimal context, impact analysis, provenance, freshness, or validation recommendation."
    collaborator_window: "Named collaborators, windows, rename, recovery, archive, close."
    core_system: "Boundary, blocklist, release/deploy contract."
    runtime_execution: "Run evidence, approval, tool/result, cleanup proof."
    provider_adapter: "Capability, tier mapping, availability."
    route_pack: "Route pack, hashes, cache keys, minimal-read."
    knowledge_footprint: "Resume pointer, opened files, gaps."
    deploy_release_audit: "deploy.yaml + release_deploy_push_audit."
    hard_isolation: "Behavioral-only unless enforcement verified."
    delegated_deployment: "deploy.yaml; one deployment_worker; exact target; deployed_file_set only."
  employee: "Read assignment/assigned paths/skill; run allowed verify; report completed/blocked/stopped."
progress_updates:
  cap: "Answer-only: none; otherwise concise per phase/30-60s and before risk/edit/verify/final."
memory: "Read .agents/docs/memory/index.md first; open at most three matching active entries; verify source_commit/content_hash/checked_at/update_trigger/supersedes/boundary; current evidence wins over stale/conflict."
multi_agent_runtime:
  trigger_when: "explicit hire/spawn/delegate/parallel-agent request"
  do_not_trigger_for: ["goal continuation","static audit","self-scoring unless requested"]
  delegation_gate: "Use independent parallel work only when value exceeds context/coordination/integration cost; keep one controller."
  modes: "sidecar; manual-detached default; cloud/automation by setup."
  roles: "explorer read-only; worker exclusive scoped edits."
  controller: "Assign, review, integrate, reconcile, report."
  batch_ack:
    use_when: "many-employee smoke/load/protocol or controller status check"
    assignment_contract: "No side effects; stable id; compact output; no file/command authority unless granted."
    compact_ack_schema: '{"id":"001","ok":true,"msg":"hello"}'
    validation: "expected id set; flag missing/duplicate/invalid-format/wrong-id/failed/running/unclosed; count requested/spawned/completed/closed; split protocol/work"
    ledger_rule: "For 2+ active employees, record summary plus exceptions."
    closeout_fields: ["requested","spawned","completed","closed","missing","duplicate","invalid_format","failed","risk"]
  fast_spawn:
    prework: "Status if needed; brief per role; expected id set/schema; no per-employee reread."
    queue_rule: "Fill slots, queue rest, refill on completion/close."
    wait_rule: "Wait only when controller needs results."
    close_rule: "Close after final report; run runtime.quiet_cleanup for parent/cwd closed subagent ids; continue after residue-zero proof."
    history_cleanup_rule: "standing cleanup authorization: parent/cwd closed subagent ids; broader needs exact auth."
    dismissal_cleanup_rule: "Dismissal uses runtime.sidebar_cleanup_success_path; record history_cleanup_evidence before clean claim."
    ledger_rule: "Append events when recovery matters; batch ack records summary/exceptions."
  lifecycle_checks:
    before: ["confirm explicit employee use","define role/read/write/command/report/close","exclusive worker scopes","record batch expected ids/schema"]
    during: ["poll as needed","dedupe reports","track ids/formats/failures/unclosed","stop at convergence/cap"]
    after: ["close runtimes","zero sqlite/unread/rollout/delayed residue","reconcile ids/git/scopes","keep ledger/runtime ignored"]
  ownership:
    required_for: "worker, 2 active employees, or controller edits near employee scope"
    normalize: "repo-relative, slash-normalized, containment-aware, case-insensitive when needed"
    hard_fail: ["overlapping active worker scopes","worker edits outside scope","controller edits active worker scope before poll/release/reconcile"]
  roster:
    project_local_ledger: ".agents/runtime/agent-ledger.jsonl"
    ledger_purpose: "Ignored advisory JSONL; not DB, deployable, XR, or XW."
    ledger_missing_or_cleared: "Recover from notices, status, reports, temp cache; missing ledger does not void evidence."
    temp_cache: "%TEMP%/codex-agent-status/<project-id>-<repo-path-hash>/<run-id>/"
    temp_cache_purpose: "External fallback for restarts, multiple controllers, weak ledger."
    required_when: ["2+ active employees","batch/many smoke","may outlive controller","manual-detached/cloud","worker writes","ownership changes","resume likely"]
    git_rule: "Never stage/commit volatile roster, events, leases, or filled validation records."
  runtime:
    completion: "Final report is completion notice."
    close: "Close exact runtime id before history cleanup."
    official_controls_first: "Use runtime close/status/archive/unsubscribe first; no hard-delete API assumed."
    claim_boundary: "Use close_requested/archived/verified_inactive unless exact runtime tools prove deletion; stale UI remains risk until delayed zero proof."
    close_success: "Clean = inactive plus official/sqlite/index/unread/rollout/delayed zero."
    residue_sources: "SQLite edges/threads; session index; unread ids; session rollouts."
    cleanup_order: "close -> exact parent/cwd/subagent ids -> clear edges/index/unread/rollouts -> delayed zero."
    cleanup_helper: "scripts/agents-cleanup.ps1 verifies/cleans an exact runtime id; destructive no-backup; never sidebar nicknames."
    quiet_cleanup: "Closeout only: exact ids, no-backup residue removal, delayed zero, report XR/XW."
    history_cleanup: "Dispatch closeout uses quiet_cleanup; otherwise require exact no-backup authorization."
    standing_cleanup_contract: "Standing closeout cleanup: close exact ids; clear matching state_*.sqlite thread_spawn_edges, session_index.jsonl, unread, and rollout records; require session-index zero, unread-state zero, and delayed zero verification; use runtime ids, not sidebar nicknames."
    sidebar_cleanup_success_path: "close -> official/sqlite/index/unread/rollout zero -> delayed zero -> UI reload if stale"
    history_cleanup_never: "Never delete nickname-only, parent/user, unrelated cwd, non-subagent rollout, backups, ledger-only ids, or unverified UI."
  report_dedupe:
    dedupe_key: "agent + runtime id + event + roster snapshot version + report hash"
    duplicate_rule: "Record matching final report once; do not reprocess duplicates."
enterprise_dispatch_runtime:
  source_of_truth: ".agents/docs/agents/dispatch.yaml owns enterprise dispatch semantics."
  trigger_when: "Org dispatch, department command, delegation, or model tier binding."
  do_not_trigger_for: ["answer-only","ordinary scoped edit","plain deploy without department command"]
  controller_assignment: "Controller assigns only department leaders with department, leader_role, objective, authority, scope, verification_profile, model_tier, report_back."
  department_leader_dispatch: "Leader may split work to allowed worker roles and owns merge before controller report."
  internal_workers: "Workers report to department leader unless escalation_record is required."
  department_report: "Leader returns one report: assignment_id, department, leader_role, execution_run_ref, worker_count, objective_result, verification, risks, escalations, isolation."
  model_policy: "Use model-policy tiers; concrete model mapping is replaceable."
  escalation: "Create escalation_record for worker bypass, scope violation, unverifiable result, missing tier, model mismatch, blocked authority."
  validation: "Verify department, leader, tier, worker role, and worker-to-controller escalation_record."
workflow_artifact_runtime:
  source_of_truth: ".agents/docs/agents/workflow-artifacts.yaml owns artifact semantics."
  trigger_when: "Create, verify, collect, simulate, or normalize artifacts, packets, gates, reports."
  do_not_trigger_for: ["answer-only","ordinary scoped edit","plain deploy","enterprise dispatch without artifact backing"]
  storage_rule: "Artifacts stay under .agents/runtime/workflows/<workflow_id>/; .workflow alias only; never commit, deploy, release, copy."
  controller_rule: "Controller integrates department reports, collection reports, or escalation records only; raw worker chatter is not evidence."
  approval_gate_rule: "write, deploy, external, destructive, or model tier upgrade needs approval gate or escalation_record."
  collection_rule: "Collect packets/results into normalized report fields before final report."
  simulation_rule: "Level 1-3 smoke must pass; Level 4 and Level 6 guardrails must block or escalate; Level 5 optional."
  execution_rule: "Workflow artifacts reference runtime execution runs for approvals, tool evidence, results, and cleanup proof."
  cleanup_rule: "Close employees, record cleanup evidence, run history cleanup, leave artifacts local or delete by explicit request."
context_compact_runtime:
  source_of_truth: ".agents/docs/agents/context-compact.yaml owns semantics."
  trigger_when: "Manual/auto compact, resume, handoff, subagent closeout, pre-commit/deploy."
  summary_rule: "v3 preserves latest_user_request, context_evidence_ref, verification_state, open_risks, next_step; source owns all fields."
  raw_content_rule: "Exclude raw transcript, raw worker chatter, long output, superseded instructions."
  runtime_rule: ".agents/runtime/compact-events.jsonl is local; never stage/deploy/release. Promote reusable verified lessons only; stale memory cannot override current evidence."
context_intelligence_runtime:
  source_of_truth: ".agents/docs/agents/context-intelligence.yaml owns minimal-context/impact-evidence semantics."
  resolver_rule: "scripts/resolve-agent-context.ps1 returns at most three files/8192 bytes; expand on gaps."
  evidence_rule: "Record provenance/confidence/hash/freshness, dependency paths, affected tests, gaps, and verification recommendation; heuristics cannot alone authorize edits or skipped verification."
  fallback_rule: "Dirty, stale, degraded, unsupported, parse-error, or conflicting evidence: expand reads + Full; never continue silently. Ignore .agents/runtime/context-intelligence/ Git state/content hashes; never stage/deploy/release it. Ledger keeps current route, verified assumptions, change boundary, remaining steps, recovery pointers; avoid full-repo rescan."
collaborator_window_runtime: ".agents/docs/agents/collaborators.yaml; leader windows only; close_rule: archive/close; runtime_rule: live ids/evidence runtime-local."
core_system_runtime: ".agents/docs/agents/core-system.yaml; boundary, canonical set, blocklist, deploy/release contract; runtime/API/provider state local."
runtime_execution_runtime: ".agents/docs/agents/runtime-execution.yaml; run, approval, tool/result, escalation, cleanup evidence; no fully deleted claim without proof."
provider_adapter_runtime: ".agents/docs/agents/provider-adapters.yaml; providers expose capabilities; departments request tiers; adapters map tiers to models."
route_pack_runtime: ".agents/docs/agents/route-packs.yaml; deterministic route id, file hashes, cache key, compact manifest."
knowledge_footprint_runtime: ".agents/docs/agents/knowledge-footprint.yaml: index-first v3, provenance, freshness, gaps, resume pointer."
scoring_batches:
  use_when: "explicit scoring or many-agent eval"
  default_size: 3
  hard_cap: "7; 10 only with explicit approval"
  convergence: "Stop when n>=3, spread<=10, majority top issue matches."
  invalid_reports: ["bad score","missing isolation line","forbidden read/write","unsupported hard-isolation/runtime claim"]
  validity_outcomes: ["accepted","needs-revision","rejected","excluded"]
  aggregation:
    cap_rule: "Cap low when range >25 or invalid reports remain."
    rule: "Median; report mean/range/excluded/confidence."
handoff:
  project_id: "lowercase repo root; normalize separators; non a-z/0-9 to hyphen; collapse/trim"
deployment:
  rule: "Use deploy.yaml; source repo stores no target status/history/commits/remotes/results."
  delegated_deployment: "Delegate exact target deployment only."
  delegated_worker: "For deploy-to-path requests, assign one deployment_worker with target path, mode, dry-run/write scope, and no app-code/OS-permission repair authority."
  upgrade: "Allowlisted redeploy: dry-run first, preserve target-owned state, update deployed_file_set."
skill_authoring: "Create .agents/skills/<name>/SKILL.md only for repeatable repo-local procedures."
maintenance:
  local_permission_boundary: "No OS permission/ownership/ACL/.git repair; report Git metadata blockers; resume after writable repo or clean clone."
  verification_profiles:
    change_aware: "validate-changes.ps1 uses ContextMode Auto; Required for release evidence; checkpoint gates never reduce."
    fast_turn: "answer-only/scoped work"
    policy_pack_edit: "changed files, canonical sources, and starter uniqueness"
    commit_tag_checkpoint: "ordinary commit/tag/version/branch-push checkpoint"
    release_deploy_push_audit: "release/deploy/protected push/broad audit/no-deduction"
    runtime_multi_agent: "real employee runtime evidence"
    collaborator_window: "thread capability evidence and runtime-local close/archive proof"
    hard_isolation: "verified enforcement evidence"
  local_only: "Use verify.file_classes.runtime_local as source; never stage/deploy/copy."
closeout:
  compact_output: "required isolation line plus directly relevant facts"
  compact_handoff: "Use context_compact fields; compact summaries are not approvals."
